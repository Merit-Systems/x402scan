{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "trigger:dev": {
      "description": "Start analytics sync jobs in development mode",
      "passThroughEnv": ["DISCORD_WEBHOOK_URL", "BASE_RPC_URL"]
    },
    "trigger:deploy": {
      "description": "Deploy analytics sync jobs to Trigger.dev",
      "passThroughEnv": ["TRIGGER_*"]
    }
  }
}
