{
  "name": "x402sync-analytics",
  "version": "1.0.0",
  "description": "Analytics cron jobs for x402scan",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "scripts": {
    "trigger:dev": "trigger dev",
    "trigger:deploy": "trigger deploy",
    "check:types": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "knip": "pnpm -w knip --workspace ./sync/analytics",
    "format": "pnpm -w format-dir ./sync/analytics",
    "check:format": "pnpm -w check:format-dir ./sync/analytics"
  },
  "dependencies": {
    "@x402scan/analytics-db": "workspace:*",
    "@x402scan/scan-db": "workspace:*",
    "@trigger.dev/sdk": "4.0.5"
  },
  "devDependencies": {
    "@x402scan/eslint-config": "workspace:*",
    "@x402scan/typescript-config": "workspace:*",
    "@trigger.dev/build": "4.0.5",
    "@types/node": "^20",
    "eslint": "^9.39.1",
    "trigger.dev": "^4.0.4",
    "typescript": "^5.9.3"
  }
}
