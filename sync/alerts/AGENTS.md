# Package guidance

Owns Trigger.dev alert synchronization jobs. Keep alert-specific runtime and deployment code inside this package.

Validate with `pnpm --filter @x402scan/sync-alerts types:check`. Run `pnpm check` before handing off repository-wide changes.
