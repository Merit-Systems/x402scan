{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "description": "Build MCP server package",
      "outputs": ["dist/**"]
    },
    "dev": {
      "description": "Start MCP server in development mode",
      "persistent": true,
      "passThroughEnv": [
        "X402_*",
        "APPDATA",
        "CODEX_HOME",
        "XDG_CONFIG_HOME",
        "INVITE_CODE",
        "CLAUDECODE",
        "CI"
      ]
    },
    "build:mcpb": {
      "description": "Build MCP server bundle for distribution",
      "dependsOn": ["^build"],
      "outputs": ["bundle/**"]
    }
  }
}
