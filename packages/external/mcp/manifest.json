{
  "manifest_version": "0.3",
  "name": "x402scan",
  "display_name": "x402scan",
  "version": "0.0.1",
  "description": "Generic MCP server for calling x402-protected APIs with automatic payment handling",
  "long_description": "Make paid API calls to any x402-protected endpoint. Automatic wallet management, v1/v2 protocol support, and verbose debugging. Pay per call with USDC on Base - no API keys needed.",
  "author": {
    "name": "Merit Systems",
    "url": "https://github.com/merit-systems"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/merit-systems/x402scan/packages/external/mcp"
  },
  "homepage": "https://x402scan.com",
  "license": "MIT",
  "server": {
    "type": "node",
    "entry_point": "server/index.cjs",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/index.cjs"],
      "env": {
        "ALLOWED_DIRECTORIES": "https://x402scan.com"
      }
    }
  },
  "compatibility": {
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": {
      "node": ">=18.0.0"
    }
  },
  "tools": [
    {
      "name": "check_balance",
      "description": "Check wallet address and USDC balance"
    },
    {
      "name": "get_wallet_address",
      "description": "Get the wallet address."
    },
    {
      "name": "check_x402_endpoint",
      "description": "Check if an endpoint is x402-protected and get pricing options, schema, and auth requirements (if applicable)."
    },
    {
      "name": "fetch",
      "description": "Fetches an x402-protected resource and handles payment automatically. If the resource is not x402-protected, it will return the raw response."
    },
    {
      "name": "discover_resources",
      "description": "Discover x402-protected resources from an origin. Fetches the /.well-known/x402 discovery document and optionally tests each resource to get pricing and requirements."
    },
    {
      "name": "authed_call",
      "description": "Make a request to a SIWX-protected endpoint. Handles auth flow automatically: detects SIWX requirement from 402 response, signs proof with server-provided challenge, retries."
    }
  ]
}
