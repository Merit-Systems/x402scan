# Package guidance

Owns the public facilitator metadata package and its published exports. Preserve its package contract and generated distribution shape.

Validate with `pnpm --filter facilitators types:check` and `pnpm --filter facilitators build`. Run `pnpm check` before handing off repository-wide changes.
