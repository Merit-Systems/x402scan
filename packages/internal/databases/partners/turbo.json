{
    "$schema": "https://turbo.build/schema.json",
    "extends": [
        "//"
    ],
    "tasks": {
        "build": {
            "env": [
                "PARTNERS_CLICKHOUSE_*"
            ],
            "outputs": [
                "dist/**"
            ]
        },
        "dev": {
            "env": [
                "PARTNERS_CLICKHOUSE_*"
            ],
            "persistent": true
        }
    }
}