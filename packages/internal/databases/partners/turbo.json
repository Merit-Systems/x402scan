{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "description": "Build partners database package",
      "env": ["PARTNERS_CLICKHOUSE_*"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "description": "Start partners database package in development mode",
      "env": ["PARTNERS_CLICKHOUSE_*"],
      "persistent": true
    }
  }
}
