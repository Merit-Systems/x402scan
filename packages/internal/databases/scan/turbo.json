{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["SCAN_DATABASE_URL*"]
    },
    "db:deploy": {
      "env": ["SCAN_DATABASE_URL*"]
    },
    "types:check": {
      "dependsOn": ["db:generate"]
    },
    "lint": {
      "dependsOn": ["db:generate"]
    }
  }
}
