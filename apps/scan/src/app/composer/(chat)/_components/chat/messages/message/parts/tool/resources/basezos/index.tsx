import { BasezosInput } from './input';
import { BasezosOutput } from './output';

import type { ResourceComponent } from '../types';

export const basezosComponents: ResourceComponent = {
  input: BasezosInput,
  output: BasezosOutput,
};
