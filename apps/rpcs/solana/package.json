{
  "name": "@x402scan/solana-rpc",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "portless",
    "dev:app": "wrangler dev src/index.ts --local",
    "deploy": "wrangler deploy src/index.ts",
    "test": "vitest run",
    "types:check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "@merit-systems/typescript-config": "catalog:foundation",
    "portless": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4.2.0"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "portless": {
    "name": "solana-rpc.x402scan",
    "script": "dev:app"
  }
}
