{
  "name": "x402-solana-rpc",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "wrangler dev src/index.ts --local",
    "deploy": "wrangler deploy src/index.ts",
    "test": "vitest",
    "check:types": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "knip": "pnpm -w knip --workspace ./apps/rpcs/solana",
    "format": "pnpm -w format-dir ./apps/rpcs/solana",
    "check:format": "pnpm -w check:format-dir ./apps/rpcs/solana"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "@x402scan/eslint-config": "workspace:*",
    "@x402scan/typescript-config": "workspace:*",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "^1.3.1",
    "wrangler": "^4.2.0"
  }
}
