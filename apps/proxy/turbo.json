{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "description": "Start proxy server in development mode",
      "env": ["PORT"]
    },
    "build": {
      "description": "Build proxy server for production",
      "env": ["PORT"],
      "outputs": ["dist/**"]
    }
  }
}
