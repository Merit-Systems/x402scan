# x402scan proxy

Run the service with `pnpm --filter @x402scan/proxy dev`. It is available at
`proxy.x402scan.localhost`; Portless may add a prefix in branch worktrees to
keep concurrent checkouts isolated.
