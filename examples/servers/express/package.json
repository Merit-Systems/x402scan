{
  "name": "express-server-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx index.ts",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "types:check": "tsc --noEmit",
    "knip": "pnpm -w knip --workspace ./examples/servers/express",
    "format": "pnpm -w format:dir ./examples/servers/express",
    "format:check": "pnpm -w format:check:dir ./examples/servers/express"
  },
  "dependencies": {
    "dotenv": "catalog:env",
    "express": "catalog:express",
    "facilitators": "workspace:*",
    "x402-express": "catalog:x402-v1"
  },
  "devDependencies": {
    "@x402scan/eslint-config": "workspace:*",
    "@x402scan/typescript-config": "workspace:*",
    "@types/express": "catalog:express",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
