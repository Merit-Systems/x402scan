{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "description": "Start Express example server in development mode",
      "env": ["ADDRESS"]
    },
    "build": {
      "description": "Build Express example server",
      "env": ["ADDRESS"]
    }
  }
}
