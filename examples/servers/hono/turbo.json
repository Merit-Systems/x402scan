{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "description": "Start Hono example server in development mode",
      "passThroughEnv": ["ADDRESS"]
    }
  }
}
